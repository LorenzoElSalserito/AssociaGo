name: Build AssociaGo Multi-Platform

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: desktop/package-lock.json

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Spring Boot JAR
        run: ./gradlew build -x test

      # Preparazione risorse per Electron
      - name: Prepare Electron resources directory
        run: |
          mkdir -p desktop/resources/backend
          find build/libs -name "*.jar" ! -name "*-plain.jar" -exec cp {} desktop/resources/backend/backend.jar \;

      # Installazione dipendenze Frontend
      
      # LINUX: Usa npm ci (veloce e sicuro)
      - name: Install Frontend Dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        working-directory: ./desktop
        run: npm ci

      # WINDOWS & MACOS: Rimuovi lockfile e usa npm install per risolvere dipendenze native
      - name: Install Frontend Dependencies (Windows/Mac)
        if: matrix.os != 'ubuntu-latest'
        working-directory: ./desktop
        run: |
          rm -f package-lock.json
          rm -rf node_modules
          npm install

      # Il comando npm run build:xxx esegue automaticamente 'prebuild' che scarica il JRE corretto per la piattaforma
      
      # --- LINUX ---
      - name: Build for Linux
        if: matrix.os == 'ubuntu-latest'
        working-directory: ./desktop
        run: npm run build:linux

      # --- WINDOWS ---
      - name: Build for Windows
        if: matrix.os == 'windows-latest'
        working-directory: ./desktop
        run: npm run build:win

      # --- MACOS ---
      - name: Build for macOS
        if: matrix.os == 'macos-latest'
        working-directory: ./desktop
        run: npm run build:mac
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: AssociaGo-${{ matrix.os }}
          path: |
            desktop/dist/*.AppImage
            desktop/dist/*.deb
            desktop/dist/*.exe
            desktop/dist/*.dmg
            desktop/dist/*.zip
          if-no-files-found: warn
          retention-days: 5
